# Postgres connection (local dev)
DATABASE_URL="postgresql://bytedance@localhost:5432/lol_system"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-random-32+-char-string"

# Default password assigned to all auto-registered captain accounts.
# Captains will be forced to change it on first login.
DEFAULT_USER_PASSWORD="lol2026"
